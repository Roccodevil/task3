Agentic Explainer

Structure scaffold for a multi-agent explainability pipeline.

To run the demo (after installing requirements):

```bash
pip install -r requirements.txt
# For Flask frontend
flask run --port=5000

# Or (development)
python app.py
```
